PROMPT: Analyze the CreateDndcInputFiles function from line 488 to 671 in /home/sakom/DNDC-Guelph-linux-port/CreateInputFiles.cpp. Do the analysis line by line and explain everything.

RESPONSE:

## Line-by-Line Analysis of `CreateDndcInputFiles` (lines 488–671)

---

### Function Overview (Plain-Language Summary)

**`CreateInputFiles()` Function:**

- **Input:** One or more `.dnd` file name(s) passed as a string argument.

- **Variables declared:**
  - `jjd` — represents the crop model approach (crop_model_approach)
  - `SF` — string filename used for building intermediate file paths
  - `MP` — string for the Manure Field file path
  - `sf`, `mp` — file-type variables (FILE pointers) corresponding to `SF` and `MP` respectively, used for writing those files
  - `DB`, `db` — string path and FILE pointer for handling DB-related intermediate files
  - `DCB` — plain char buffer for building Crop Rotation file paths

- **Crop Rotation initialization loop (kx10 = 1 to 10):**
  - Each iteration builds the CropRotation file path into `DCB` using the `INTERMANAGE` base path combined with the current iteration index
  - If the file cannot be opened with write permissions, an error is printed via `note()`
  - Writes three values to the file: the current iteration number, `1`, and `1`
  - Closes the file before the next iteration

---

### Function Signature & Local Variable Declarations (488–496)

```cpp
void CreateDndcInputFiles(char *InputFileName)   // 488
```
Takes a C-string path to an input file. Returns nothing. This function creates all the intermediate files DNDC needs before the simulation starts.

```cpp
int jjd;                          // 490 — declared but never used in this range; likely a leftover
CString SF, MP;                   // 491 — Windows MFC string type used for building file paths (does NOT work on Linux)
FILE* sf, *mp;                    // 492 — standard C file handles for the above paths
CString DB;                       // 494 — another Windows MFC string for database/path building
FILE *db;                         // 495 — file handle for DB
char DCB[200];                    // 496 — plain C char buffer for CropRotation file paths (Linux-safe)
```

---

### CropRotation File Initialization (497–504)

```cpp
for(int kx10=1; kx10<=10; kx10++)               // 497 — loop 10 crop rotation slots
{
    sprintf(DCB, "%s_1/CropRotation_%d.txt", INTERMANAGE, kx10); // 499 — builds path like <INTERMANAGE>_1/CropRotation_1.txt
    db=fopen(DCB, "w");                          // 500 — create/overwrite the file
    if(db==NULL) note(1,DCB);                    // 501 — error if open failed; note() is the error handler
    fprintf(db, "%d %d %d", kx10, 1, 1);        // 502 — writes: slot_number 1 1 (rotation ID + two defaults)
    fclose(db);                                  // 503
}
```
Creates 10 CropRotation_*.txt empty placeholder files. Each file contains three integers: the slot index and two hard-coded 1s (likely "number of crops" and "rotation flag").

---

### OpenFile.txt Initialization (506–509)

```cpp
DB.Format("%s/Inter/OpenFile.txt", OUTPUT);     // 506 — builds path under OUTPUT/Inter/
db=fopen(DB, "w");                              // 507
fprintf(db, "%d", 1);                           // 508 — writes the integer 1 (signals "file is open/active")
fclose(db);                                     // 509
```
Creates a file that other parts of the code read to know the inputs are ready.

---

### `#ifdef MANURE` Block — Blank Manure Files (512–598)

This entire block is conditionally compiled only when the MANURE preprocessor flag is set.

#### ManureHouse Files (514–542)

```cpp
for(int m_FeedlotYear=0; m_FeedlotYear<=10; m_FeedlotYear++)  // 514 — 11 years (0..10)
{
    for(int iy3=0; iy3<=10; iy3++)                             // 516 — 11 sub-year indices
    {
        SF.Format("%s/Inter/ManureHouse_%d_%d.txt", OUTPUT, m_FeedlotYear, iy3); // 518
        sf=fopen(SF, "w");                        // 519
        if(sf==NULL) note(1, SF);                 // 520

        fprintf(sf, "%d  %d\n", 0, 1);           // 522 — animal count=0, type=1
        fprintf(sf, "%3d  %8.2f  %8.2f  %8.4f  %8.4f\n", 0,0.0,0.0,0.0,0.0); // 523 — housing params all zero
        fprintf(sf, "%f  %d\n", 0.0, 0);         // 524 — 0=slatted floor, 1=concrete floor (comment in code)
        fprintf(sf, "%d  %f  %d  %f\n", 0,0.0,0,0.0); // 525 — bedding type (0=none,1=straw,2=sawdust,3=dry manure,4=sand)
        fprintf(sf, "%d  %f  %s\n", 0,0.0,"None"); // 526 — ventilation: 0=natural, 1=fan
        fprintf(sf, "%f\n", 0.0);                 // 527 — manure removal rate
        fprintf(sf, "%f %f %f %f %f\n", 0.0,...); // 528 — five nutrient/gas parameters (all zero)
        fprintf(sf, "%f %f %f %f %f\n", 0.0,...); // 529 — five more parameters (all zero)
        fprintf(sf, "%d %f\n", -1, 0.0);         // 530 — terminator: -1 flags end-of-record
        fclose(sf);                               // 532
    }

    // TotalFeedLots per year (535–541)
    TotalFeedlots.Format("%s/Inter/TotalFeedLots_%d", OUTPUT, m_FeedlotYear); // 537
    TFL=fopen(TotalFeedlots, "w");
    fprintf(TFL, "%d", 1);                        // 540 — always starts with 1 feedlot
    fclose(TFL);
}
```
Creates 11×11 = 121 blank ManureHouse files and 11 TotalFeedLots files. All values are zeros/defaults — these are empty placeholder files that are later overwritten with real animal housing data if any feedlot simulation is active.

#### ManureGraze.txt (544–548)

```cpp
SF.Format("%s/Inter/ManureGraze.txt", OUTPUT);
fprintf(sf, "%d  %d  %d  %f\n", 0, 8, 0, 0.0);
```
Empty placeholder for grazing manure. Fields: grazing flag=0, number of days=8 (default grazing period), type=0, rate=0.0.

#### ManureLagoon.txt (554–566)

```cpp
#ifdef CONSOLE
    SF.Format("%s/Inter/ManureLagoon.txt", OUTPUT);        // 555 — console build path
#else
    SF.Format("%s/Result/Inter/ManureLagoon.txt", ROOTDIR); // 557 — GUI build uses ROOTDIR
#endif
fprintf(sf, "%f  %f  %d  %d  %d\n", 0.0,0.0,0,0,0);     // 563 — lagoon volume, area, type, cover, liner (all zero)
fprintf(sf, "%d %d %f  %f  %f\n", 0,0,0.0,0.0,0.0);      // 564 — pH, temp, nutrient concentrations
fprintf(sf, "%d %f\n", 0,0.0);                             // 565 — pump flag, pump rate
```
Note the split path: CONSOLE builds use OUTPUT, GUI builds use ROOTDIR. This will cause problems on Linux — a Linux port should use one consistent path for both.

#### ManureCompost.txt (568–573)

```cpp
fprintf(sf, "%f  %d  %d  %f  %f\n", 0.0,0,0,0.0,0.0);
```
Five fields: compost mass, type, turning frequency, C:N ratio, moisture — all set to zero as empty placeholders.

#### ManureDigester.txt (575–581)

```cpp
fprintf(sf, "%d  %f  %f\n", 0,0.0,0.0);    // digester type, volume, temperature
fprintf(sf, "%f  %f  %f\n", 0.0,0.0,0.0);  // gas capture rate, CH4 yield, NH3 emission
```

#### ManureField.txt (583–588)

```cpp
fprintf(sf, "%f", 0.0);    // single float — total manure applied to field (kg N/ha)
```

#### ManureCropNumber (590–597)

```cpp
XFF.Format("%s/ManureCropNumber", INTER);
fprintf(xff, "%d  %d  %d\n", 0, 0, 0);    // three integers: crop slot indices linked to manure stream
```

---

### Variable Declarations for Input Parsing (605–611)

```cpp
int j, cropn, MissFlag=0, years, ifdaily;                // 605
int floon, ShallowFlood, WaterControl,                    // 606
    FlooMonth1, FlooDay1, FlooMonth2, FlooDay2;
float FlooN, WaterGetherIndex, WaterLeakRate,             // 607
      latitude, FloodWaterN;
float m_IniWT, m_LWTceasingSurfFlow,                      // 608
      m_LWTceasingGroungFlow, m_WatershedIndex,
      m_SurfOutflowIntensity, m_GroundOutflowIntensity;
char notes[255], tmp[255], ffname[300], sitename[200];    // 609
FILE *fp;                                                  // 610
CString txt;                                               // 611
```
All the variables needed to parse the legacy text-format input file: site metadata, flood/water-table parameters, and file name buffers.

---

### Opening the Input File (613–619)

```cpp
char fname[300];
sprintf(fname, "%s", InputFileName);       // 614 — copy arg into local buffer
sprintf(ffname, "%s", fname);              // 616 — redundant copy (comment in code notes this: "why 2?")
fp=fopen(ffname,"r");                      // 618 — open for reading
if(fp==NULL) note(0, ffname);             // 619 — note(0,...) = fatal error (vs note(1,...) = warning)
```
The double-copy of the filename is unnecessary — `ffname` ends up identical to `fname`. Likely leftover from an older version of the code.

---

### Site Base Variables Reset (623–633)

```cpp
strcpy(sitename,"");     // 623 — clear site name
latitude  = 0;           // 625
float longitude = 120.0; // 626 — default longitude (120°E — originally calibrated for China)
int totalday = 0;        // 627
years    = 0;            // 628
ifdaily  = 0;            // 629
int Line = 1;            // 633 — line counter for error reporting
```
`longitude = 120.0` is a hard-coded default inherited from DNDC's original Chinese calibration. For a Guelph (Ontario) deployment this would be ~−80°.

---

### Format Detection and Routing (635–644)

```cpp
fscanf(fp,"%s",notes);                                     // 635 — read first token
if(strcmp(notes, "DNDC_Input_Parameters")==0)              // 636 — NEW format check
{
    fclose(fp);                                             // 639
    void ReadDNDfile(char *fname, int UseID);               // 640 — forward declaration INSIDE function body (non-standard but legal in C)
    ReadDNDfile(fname, 0);                                  // 641 — parse new-format file
    CreateOutputFilesFromIData();                           // 642 — generate output placeholder files from parsed data
    return;                                                 // 643 — skip legacy parser below
}
```
This is the shortcut for new-format files. If the file starts with `DNDC_Input_Parameters`, it calls the newer `ReadDNDfile` + `CreateOutputFilesFromIData` functions and returns early. The inline forward declaration on line 640 is unusual style — it works but is better placed at file scope.

---

### Legacy Format Header Validation (645–668)

```cpp
if(strcmp(notes, "Input_Parameters:")!=0) { Toendd("Input_Parameters:", fname);}  // 645
```
If the first token is neither `DNDC_Input_Parameters` nor `Input_Parameters:`, the file format is unrecognised — `Toendd()` is called (likely prints an error and exits).

```cpp
fscanf(fp,"%s",notes);   // 647 — skip "--------------------" separator line
fscanf(fp,"%s",notes);   // 648 — skip another token (comment notes this is not a proper skip method)
fscanf(fp,"%s",notes);   // 650 — read next token
```
Three successive `fscanf` calls are used to skip the dashed separator. This is fragile: if the separator has more/fewer tokens than expected the parser drifts. The comment on line 648 already flags this.

```cpp
if(strcmp(notes, "Site_name:")!=0)        // 651 — if token is NOT the label, it IS the value
    sprintf(sitename, "%s", notes);       // 653 — site name was on same line as previous token
else
    fscanf(fp,"%s", sitename);            // 656 — site name is on the NEXT token
```
Handles two layout variants: `Site_name: value` and `value` appearing inline.

```cpp
fscanf(fp,"%s %d", notes, &years);                            // 659
if(strcmp(notes, "Simulated_Year:")!=0) Toendd(...);          // 660

fscanf(fp,"%s %f", notes, &latitude);                         // 663
if(strcmp(notes, "Latitude:")!=0) Toendd(...);                 // 664

fscanf(fp,"%s %d", notes, &ifdaily);                          // 667
if(strcmp(notes, "Daily_Record:")!=0) Toendd(...);             // 668
```
Each pair: read a label+value, then validate the label matches expected keyword. If not, call `Toendd()` to abort with a parse error. After these three reads we have: simulation length (years), site latitude, and daily/annual output flag.

---

---

## Continued Analysis: Lines 671–2601

---

### MANURE block — variable declarations (671–693)

Only compiled when `MANURE` flag is set.

**673**: `HouseInputFormat, AnimalTypes, AnimalType[10]` — integers for how the animal housing data is provided (format type), how many animal types, and an array of type codes.

**674**: `HouseInputFileName[200], IndoorClimateFile[300]` — char buffers for optional external file names for housing and climate data.

**675**: `AnimalHeads[10], FeedRate[10], FeedProtein[10], FeedTP[10]` — arrays of floats for number of animals, how much they eat, protein fraction, and total phosphorus fraction. One slot per animal type (up to 10).

**676**: `m_FloorArea, m_BedCN, m_VentRate, m_BedRate, m_BedFrequency, PenBedFrequency` — floor area of the barn, carbon-to-nitrogen ratio of bedding, ventilation rate, bedding addition rate, how often bedding is changed, and same for the outdoor pen.

**677**: `m_FloorSurface, m_BedType, m_Ventilation` — integers: floor type (slatted vs concrete), bedding material type, ventilation type (natural vs fan).

**678–680**: Pen variables — same concepts as the house variables above but for the outdoor pen. Supports up to 15 animal types (`[15]` arrays).

**681–683**: Grazing variables — `GrazeInputFormat`, `GrazeAnimalType`, `GrazeTimes`, start/end month and day arrays sized 366 (one per day of year maximum), animal heads and hours per day for grazing, and total grazing area.

**684–685**: Lagoon variables — capacity, surface area, arrays for what fraction goes to field/market/remains after each drain event, drain dates, application method and depth.

**686**: Compost variables — density, litter amount and C:N ratio, and fractions going to field/market/remaining.

**687**: Digester variables — capacity, CH4 yield, operating days, and fractions going to field/market/remaining.

**688**: `m_Temp, m_Flash, HouseID, m_CompostCover, m_CompostRemoveTimes, CompostRemoveMonth, CompostRemoveDay` — integers for digester temperature, flushing on/off, which house this is, compost covered or not, how many times compost is removed per year and when.

**689**: `DigesterToMarket, DigesterRemain, m_FlushWater, LiquidFraction` — floats for what fraction of digester output goes to market/stays, how much flush water is used, and liquid fraction of manure.

**690**: `HouseManureOutDays` and urine/feces fractions: `m_UC` (urine C), `m_UL` (urine litter), `m_UD`, `m_UF`, `m_UR`, and the feces equivalents `m_FC` through `m_FR`. These track how much carbon/nitrogen ends up in different fractions of the manure.

**691**: Same fraction variables for the pen.

**692**: Same for grazing, plus `FieldArea` (total land area receiving manure) and `jf` (a scratch float used for old-format reading).

**693**: `NutritionModel` — integer selecting which animal nutrition model to use.

**695–698**: Commented-out re-declarations of `MP, mp, SF, sf` — these were already declared at the top of the function. The comments confirm they were removed here to avoid redeclaration.

**700**: `m_FeedLots, m_FeedlotYears, m_FeedlotYear` — how many feedlot operations exist, how many years of feedlot data, and the loop counter.

---

### Reading feedlot years and writing ManureHouse files (702–799)

**702–703**: Two `fscanf` calls skip a section header token (likely `"Feedlot:"` or similar separator).

**704**: `fscanf(fp,"%s %d", notes, &m_FeedlotYears)` — reads how many years of feedlot data are in the file.

**706**: Loop `m_FeedlotYear = 1..m_FeedlotYears` — one iteration per feedlot year.

**709**: `fscanf(fp,"%s %d", notes, &m_FeedLots)` — reads how many feedlot operations exist for this year.

**711–717**: Opens `TotalFeedLots_<year>` and writes the real `m_FeedLots` count into it, replacing the placeholder `1` that was written earlier in the blank-file setup phase.

**719**: Inner loop `iy4 = 1..m_FeedLots` — one iteration per feedlot.

**721**: Reads `HouseID` — the ID number of this particular house.

**722–725**: Reads `HouseInputFormat`. If the read fails (returns != 2), calls `AfxMessageBox("No livestock data")`. **Note**: `AfxMessageBox` is a Windows MFC function — not available on Linux. Must be replaced with `fprintf(stderr, ...)` or similar.

**728–729**: If format is 1 or 2, reads an external file name into `HouseInputFileName`. Format 1/2 means animal data comes from a separate file rather than inline.

**730–731**: If format is 3, reads `NutritionModel` integer — uses a built-in nutrition model.

**732–741**: Else (format not 1/2/3 — i.e., inline data): reads `AnimalTypes`, then `AnimalType[1]`, `AnimalHeads[1]`, skips a `"feed"` label, then reads `FeedRate[1]`, `FeedProtein[1]`, `FeedTP[1]`.

**742–769**: Reads the housing details regardless of format: floor section (`m_FloorArea`, `m_FloorSurface`), bedding section (`m_BedType`, `m_BedCN`, `m_BedFrequency`, `m_BedRate`), ventilation section (`m_Ventilation`, `m_VentRate`, `IndoorClimateFile`), manure removal section (`HouseManureOutDays`, then 10 fractions `m_UC` through `m_FR`), and flushing section (`m_Flash`, `m_FlushWater`, `LiquidFraction`).

**773–795**: Writes the real `ManureHouse_<year>_<lot>.txt` file with all the data just read. This overwrites the blank placeholder written at the start. Format mirrors the blank file structure but with real values. The `if(AnimalHeads[1]>0)` guard is commented out — so the file is always written even if there are no animals.

**794**: Writes `-1` flag at the end — but note the format changed from the blank file: blank had `%d %f` but this write uses `%d %f %f` (three values). The blank placeholder and the real file have slightly different record layouts — this may cause a parsing mismatch downstream.

---

### Reading and writing lagoon data (807–851)

**807–811**: Reads four lagoon fields: skips `"lagoon"` header, then reads `m_LagoonCapacity`, `m_LagoonSurfaceArea`, `m_LagoonCover` (covered or not), `m_LagoonRain` (whether rain enters the lagoon).

**813–821**: Reads the next token. If it is `"SlurryRemovalFrequency"` (old format): reads a single float `jf` (ignored), sets `m_LagoonDrainTimes = 0`, and reads one set of `LagoonToField[1]`, `LagoonToMarket[1]`, `LagoonRemain[1]`.

**823–836**: New format: reads `m_LagoonDrainTimes`, then loops that many times reading drain month/day, fractions to field/market/remaining, application method, and application depth for each drain event.

**841–851**: Writes `ManureLagoon.txt` with the real lagoon data. First line: capacity, area, cover, rain flag, drain count. Then one line per drain event: month, day, fractions, then method and depth on a second line. **Note**: the path here is hardcoded as `OUTPUT/Inter/ManureLagoon.txt` regardless of CONSOLE/GUI — this is an improvement over the blank-file section but inconsistent with it.

---

### Reading and writing compost data (855–880)

**855–860**: Reads compost section: skips `"compost"` header, then reads `m_CompostDensity`, `m_CompostCover`, `m_CompostRemoveTimes`, `m_LitterAmount`, `m_LitterCN`.

**864–879**: Writes `ManureCompost.txt`: first line has the five values just read. Then loops `m_CompostRemoveTimes` times, reading `CompostRemoveMonth`, `CompostRemoveDay`, and fractions to field/market/remaining for each removal event, writing each set to the file.

---

### Reading and writing digester data (884–900)

**884–890**: Reads digester section: skips `"digester"` header, then reads `m_Temp`, `m_DigesterCH4`, `m_DigesterDays`, `DigesterToField`, `DigesterToMarket`, `DigesterRemain`.

**894–900**: Writes `ManureDigester.txt` with two lines: temperature/CH4 yield/days, then the three destination fractions.

---

### Reading and writing field application data (902–946)

**902–903**: Reads `"land application"` header and `FieldArea`.

**905–912**: If `FieldArea > 0`, opens `ManureField.txt` (using `MP`/`mp`) and writes the field area. This is the one place `MP` and `mp` are actually used.

**917–919**: Opens `ManureFieldCrops` (no extension, under `OUTPUT/Inter/`). **Note**: line 919 checks `if(mp==NULL)` — this is a bug. It should check `if(xff==NULL)`. It checks the wrong variable.

**921**: Declares `CropOrder, FarmCropType, TotalFields=0, TotalFarmYears=0` — note `TotalFarmYears` is declared zero and never updated before being written to the file on line 925.

**924**: Reads `TotalFields` from the input file.

**925**: Writes `TotalFields` and `TotalFarmYears` (always 0) to `ManureFieldCrops`.

**927–939**: Loop over each field: reads `CropOrder`, `FarmCropType`, `FarmCropHa`, `ManuFraction`. Writes them to the file. Then creates a directory `OUTPUT/Inputs/FarmCrop_<n>` using `_mkdir`. **Note**: `_mkdir` is a Windows function; Linux uses `mkdir(path, mode)` with a second argument.

**940**: Closes `ManureFieldCrops`.

**942–946**: Re-opens and writes `ManureCropNumber` with `TotalFields`, `CropOrder` (last iteration's value), `FarmCropType` (last iteration's value). This records the final crop order and type from the loop.

**948–956**: Commented-out block that would have written `ManureFieldAll.txt`. Removed but left in source.

**957**: `#endif` — end of the `MANURE` block.

---

### Climate data section (960–1016)

**960–968**: Declares `ClimateFileType, ClimateFileNumber, if1File` (integers), `NO3NH4_Rainfall, NH3_Atmosphere, BaseCO2, CO2IncreaseRate` (floats), and `ClimateFilename[300][200]` — an array that can hold up to 300 climate file names, each up to 200 characters.

**966–967**: Two `fscanf` calls skip a `"----"` separator and a `"Climate data:"` header.

**970–971**: Reads `ClimateFileType` and checks label. This integer controls what format the climate data files are in.

**974–975**: Reads `NO3NH4_Rainfall` — amount of nitrate+ammonium deposited per mm of rain.

**978–979**: Reads `NH3_Atmosphere` — background atmospheric ammonia. The label check is commented out (no abort if label is wrong).

**982–983**: Reads `BaseCO2` — starting CO2 level in ppm.

**986–987**: Reads `ClimateFileNumber` — how many climate files are listed. **Note**: the label check uses `"Climate_file_count="` but the error message says `"Climate_file_count:"` — small inconsistency.

**995–1002**: Loop reads each climate file name. Format is `<index> <filename>`. The index `j` is read but not used — only the filename is stored in `ClimateFilename[iy1]`.

**1004–1016**: Reads `"Climate_file_mode"` token. If present, reads `if1File` (0=one file per year, 1=use same file for all years) and `CO2IncreaseRate`. If not present, sets both to 0 and sets `MissFlag=1`.

---

### Soil parameters section (1019–1150)

**1019–1020**: If `MissFlag==0`, skips a `"----"` separator. Resets `MissFlag=0`.

**1022–1024**: Declares all the soil variables: texture class, land use type, density, pH, organic carbon at surface, clay fraction, bypass flow, litter carbon, humads carbon, humus carbon, NO3 and NH4 concentrations, passive C:N ratio, soil moisture, and lateral water influx factor.

**1028–1029**: Reads `Soil_Texture` — integer 1–13 representing texture class (sand, loam, clay, etc).

**1032–1033**: Reads `Soil_landuse` — integer land use type.

**1036–1037**: Reads `Soil_Density` in g/cm³.

**1040–1041**: Reads `Soil_pH`.

**1045–1046**: Reads `Soil_OCatSurface` — soil organic carbon fraction at the surface layer.

**1049–1050**: Reads `Soil_Clay` — clay fraction (0 to 1).

**1053–1054**: Reads `Soil_BypassFlow` — how much water bypasses the soil matrix through cracks.

**1057–1058**: Reads `Soil_Litter` — fraction of total SOC that is litter.

**1061–1062**: Reads `Soil_humads` — fraction of SOC that is active humus.

**1065–1066**: Reads `Soil_humus` — fraction of SOC that is stable humus.

**1069–1070**: Reads `Soil_NO3` in mg N/kg soil.

**1073–1074**: Reads `Soil_NH4` in mg N/kg soil.

**1077–1080**: Reads what should be `PassiveCCN` but the label check is commented out. Then immediately overwrites the read values: `Soil_moisture = 0.5` (hardcoded) and `PassiveCCN = 500.0` (hardcoded). So whatever was in the file for this field is ignored.

**1082–1084**: Reads `LateralInflux`. If the label token is `"Temperature"` (old field), sets `LateralInflux = 1.0`. Also forces minimum of 1.0.

**1087–1090**: Declares advanced soil physics variables: field capacity, wilting point, saturated hydraulic conductivity, porosity, SOC decay rate parameters, microbial index, highest water table depth, impeding layer depth, slope, soil quality.

**1092–1116**: Reads optional `"Field_capacity"` block. If the token is not `"Field_capacity"`, all these variables get hard-coded defaults (0.0 for most, 1.0 for decay rates, etc.) and `MissFlag=1`. This is a backward-compatibility fallback for older input files that don't have this block.

**1117–1150**: If `"Field_capacity"` was found, reads all variables: `Fldcap`, `Wiltpt`, `Sks` (hydraulic conductivity in m/hr), `Soil_Porosity`, `SOCa`, `SOCb` (SOC depth decay factors), three DC (decomposition coefficient) values, two C:N ratios, passive carbon pool, microbial index, highest water table, impeding layer depth, slope. Then checks for optional `"Salinity"` token — if present reads `Soil_Quality`; otherwise `Soil_Quality = 0.0`. Enforces minimum `Sks = 0.015`.

---

### Crop data section — setup (1152–1181)

**1153–1155**: Declares crop rotation variables: `ri` (rotation ID), `yc` (year within cycle), `db_CycleYear[20]` (how many years per cycle for each rotation), `FCTT` (farm crop counter), `db_Rotation_Number`, `FarmMode`, `FarmCropNumber`.

**1157**: `CString FCT60` — Windows path string for the current farm crop directory. Linux porting issue.

**1160–1161**: `CString CropDB` and `FILE *cropdb` — path and handle for crop rotation detail files.

**1163–1164**: Skips a separator if `MissFlag==0`. Resets `MissFlag`.

**1166**: `NewFarmCrop:` — a `goto` label. The code can jump back here to read another farm's crop data (line 2067).

**1168**: `FCTT++` — increment farm crop counter.

**1169**: `FCT60.Format("%s_%d", INTERMANAGE, FCTT)` — builds the path for this farm's management folder, e.g. `<INTERMANAGE>_1`.

**1170**: `_mkdir(FCT60)` — creates that directory. Windows function; replace with `mkdir(path, 0755)` on Linux.

**1172–1173**: Reads and checks `"Crop_data:"` header.

**1175–1176**: Reads `db_Rotation_Number` — how many crop rotations are defined.

**1178–1181**: Writes `CropRotation.txt` in the farm folder with just the rotation count on one line.

---

### Crop rotation outer loop (1188–2056)

Loop `ix1 = 1..db_Rotation_Number` — one pass per rotation.

**1190–1193**: Declares per-rotation/per-year variables for tillage and manure operations.

**1195–1196**: Reads `Rotation_ID=` and its integer value into `ri`.

**1202–1203**: Reads `Totalyear=` into `db_TotalYear` — total simulation years for this rotation.

**1206–1207**: Reads `Years_Of_A_Cycle=` into `db_CycleYear[ix1]` — how many years before the crop pattern repeats.

**1209–1214**: Writes `CropRotation_<ix1>.txt` with three lines: rotation ID, total years, years per cycle.

**1217**: Inner loop `j = 1..db_CycleYear[ix1]` — one pass per year within the cycle.

**1222–1230**: Opens `CropRotation_<ix1>_<j>.txt`. If it fails, shows `AfxMessageBox` (Windows-only) and exits. **Linux porting issue.**

---

#### Reading crop definitions (1232–1426)

**1232–1234**: Reads `YearID_of_a_cycle=` and its integer `yc`.

**1242–1243**: Reads `Crop_total_Number=` into `cropn` — how many crops grown in this year.

**1246**: Writes `cropn` to the crop file.

**1252–1266**: Declares per-crop variables: type, plant/harvest dates, yield, residue, growth parameters, photosynthesis efficiency and max, tree age, leaf/grain/shoot/root fractions, C:N ratios, temperature degree days, water use, optimal temperature, nitrogen fixation, vascularity, LAI max, frost kill threshold, and several alfalfa-specific parameters.

**1268**: Loop `k = 1..cropn` — one pass per crop in this year.

**1276–1277**: Reads `Crop_Type=` integer — which crop species (coded number).

**1280–1281**: Reads `Plant_time=` with month and day.

**1284–1285**: Reads `Harvest_time=` with month and day.

**1288–1289**: Reads `Year_of_harvest=` — which year of the cycle the crop is harvested (for multi-year crops).

**1292–1293**: Reads `Ground_Residue=` float.

**1296–1297**: Reads `Yield=` float.

**1300–1309**: Reads next token. If it is `"Leaf_fraction="` reads that float. Otherwise assumes it is `"Rate_reproductive="` and reads `db_GrowthReproductive`, leaving `db_LeafFraction = 0.0`. Handles old vs new format.

**1313–1323**: Same pattern for `"Leaf_CN="` vs old `"Rate_vegetative="` field.

**1327–1328**: Reads `db_PsnEfficiency` (photosynthesis efficiency). Label check commented out.

**1331–1332**: Reads `db_Transplant` integer. Label check commented out.

**1335–1336**: Reads `db_TreeAge` float. Label check commented out.

**1339–1340**: Reads `"Cover_crop="` integer.

**1343–1401**: Reads `"Perennial_crop="`. If label is wrong, calls `AfxMessageBox("Error in Input data")` — **Windows-only, Linux porting issue**. If correct, reads: `db_PerennialCrop`, then grain/shoot/root fractions, their C:N ratios, `db_TDD` (degree days to maturity), `db_Water` (water use coefficient), `db_OptT` (optimal temperature), `db_Nfix` (N fixation rate).

**1363–1367**: If `db_LeafFraction` is still 0.0, sets it to half of `db_ShootFraction` and splits shoot equally. This is a default calculation for crops where leaf fraction was not given.

**1369–1372**: If `db_LeafCN` is still 0.0, copies it from `db_ShootCN`.

**1376–1383**: Reads optional `"Vascularity="`. If not present, sets 0.0 and `MissFlag=1`.

**1385–1401**: Reads tree-specific parameters: `db_TreeAge`, `db_TreeCurrentAge`, `db_TreeLeafMax`, `db_TreeLeafMin`, `db_RootShape`, `db_GrainFill`, `db_LAImax`, `db_FrostKill`, then four alfalfa parameters (`AlfCHRMX`, `ALFCDRMX`, `ALFCTMX`, `ALFPDFMX`), then root respiration parameters (`Root1`, `Root2`, `AGResp1`, `AGResp2`).

**1404–1424**: Writes all crop data to the file: crop ID, type, plant date, harvest date, harvest year, residue, yield, then growth parameters, cover crop flag, perennial flag, biomass fractions, C:N ratios, physiology parameters, tree parameters, root shape and grain fill, LAI and frost kill, alfalfa parameters, root respiration parameters.

**1428**: Closes crop file.

---

#### Tillage (1430–1464)

**1432**: Builds path `CropTill_<ix1>_<j>.txt`.

**1434–1440**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1442–1444**: Reads `"Tillage_number="` into `tilln`, with `MissFlag` handling.

**1448**: Writes `tilln` to file.

**1452–1463**: Loop `kx1 = 1..tilln`: reads each tillage event — `Tillage_ID=` (just the integer, label checked), `Month/Day/method=` with three integers. Writes month, day, method to file.

**1464**: Closes tillage file.

---

#### Fertilization (1466–1595)

**1468**: Builds path `CropFert_<ix1>_<j>.txt`.

**1470–1476**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1478–1480**: Declares fertilization variables: event count, dates, method, option, amounts for eight fertilizer types (nitrate, ammonium bicarbonate, urea, anhydrous ammonia, NH4NO3, (NH4)2SO4, (NH4)2HPO4), application depth, and inhibitor/urease parameters.

**1482–1483**: Reads `Fertil_number=` into `fertn`. Writes it to file.

**1488**: If `fertn != -1` (normal fertilization, not fertigation):

**1490**: Loop `k = 1..fertn` — one event per iteration.

**1492–1494**: Reads fertilization event ID with `MissFlag` handling.

**1500–1501**: Reads `Month/Day/method=` with three integers.

**1504–1505**: Reads `Depth=` float.

**1508–1533**: Reads eight fertilizer type amounts one at a time, each with label check.

**1536–1538**: Writes month, day, method, then all eight amounts plus depth to file.

**1540–1561**: Reads slow-release and inhibitor parameters: `Release_rate=`, `Inhibitor_efficiency=`, `Inhibitor_duration=`. Then optional `Urease_efficiency=` and `Urease_duration=` — if not present, both set to 0.0 and `MissFlag=1`.

**1564–1565**: Writes the five slow-release/inhibitor parameters to file.

**1568–1583**: After the loop, reads optional `"FertilizationOption="`. If not found sets `FertOption=0` and `MissFlag=1`. Writes `FertOption` to file.

**1587–1593**: If `fertn == -1` (fertigation mode): reads a fertigation file name and writes it to the file.

**1595**: Closes fertilization file.

---

#### Manure application (1597–1664)

**1599**: Builds path `CropManu_<ix1>_<j>.txt`.

**1601–1607**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1609–1611**: Reads `Manure_number=` into `manun` with `MissFlag` handling.

**1616**: Writes `manun` to file.

**1620**: Loop `kx2 = 1..manun` — one manure event per iteration.

**1622–1624**: Reads event ID with `MissFlag` handling.

**1629–1630**: Reads `Month/Day=` with two integers.

**1633–1634**: Reads `Amount/C|N_ratio=` with two floats.

**1637–1638**: Reads `Type=` integer — manure type code.

**1641–1648**: Reads optional `"Method="`. If not present, sets `ManuMethod=0` and `MissFlag=1`.

**1650–1656**: Reads seven more manure parameters: application depth, N content, NH4 content, NO3 content, application hour, dry matter, pH.

**1658–1661**: Writes month/day, then amount/CN/type/method/depth, then N/NH4/NO3/hour/DM/pH to file.

**1664**: Closes manure application file.

---

#### Plastic mulch / weed control (1666–1712)

**1668**: Builds path `CropPlastic_<ix1>_<j>.txt`.

**1670–1676**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1678–1679**: Declares weed variables: event count, weed option, start/end dates, cover fraction.

**1680–1682**: Reads `weedn` (number of weed/mulch events) with `MissFlag` handling.

**1684–1685**: Reads `WeedOption` integer and immediately writes `weedn` and `WeedOption` together.

**1687**: Loop `kx3 = 1..weedn`.

**1689–1695**: Reads event ID, start date, end date, and `CoverFraction`.

**1696–1703**: If `"CoverFraction="` label not found, sets `CoverFraction=1.0` and `MissFlag=1`. Otherwise reads the float.

**1707–1709**: Writes start date, end date, and cover fraction to file.

**1712**: Closes plastic mulch file.

---

#### Flooding (1714–1865)

**1715**: Builds path `CropFloo_<ix1>_<j>.txt`.

**1717–1723**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1725–1733**: Reads `floon` (number of flood events) with `MissFlag` handling.

**1738–1739**: Reads `FloodWaterN` — nitrogen concentration of flood water.

**1742–1743**: Reads `Water_control=` integer — whether water table is managed.

**1746–1747**: Reads `Leak_rate=` float — how fast water leaks out.

**1750**: Writes `floon`, `FloodWaterN`, `WaterControl`, `WaterLeakRate` as the header line.

**1752**: Loop `kx4 = 1..floon`.

**1754–1755**: Reads flooding event ID.

**1758–1759**: Reads `Flood_Month/Day=` — when flooding starts.

**1762–1763**: Reads `Drain_Month/Day=` — when flooding ends.

**1766–1767**: Reads `FlooN` — N concentration during this specific flood event. Label check commented out.

**1770–1771**: Reads `Shallow_flood=` integer.

**1774–1777**: Writes start date, end date, FlooN, and shallow flood flag to file.

**1780–1815**: Reads optional `"Water_gather="`. If not found, sets `WaterGetherIndex=1.0` and `MissFlag=1`. Writes it. Then reads optional `"WT_file="` — path to a water table file. If not found, sets `"None"`. Writes it.

**1820–1860**: Reads optional `"Empirical_parameters="` — six water table model parameters. If not found, all six are set to 0.0 and `MissFlag=1`. Two separate branches handle whether `MissFlag` was set before reaching this point, but both do the same thing.

**1862–1863**: Writes the six water table parameters.

**1865**: Closes flooding file.

---

#### Irrigation (1867–1926)

**1868**: Builds path `CropIrri_<ix1>_<j>.txt`.

**1870–1876**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1878–1879**: Declares `irrin, IrriMode, IrriMonth, IrriDay, IrriMethod` and `IrriIndex, IrriAmount`.

**1881–1883**: Reads `irrin` with `MissFlag` handling.

**1887–1888**: Reads `Irrigation_type=` integer.

**1891–1892**: Reads `Irrigation_Index=` float — a threshold index (e.g. WFPS fraction) that triggers auto-irrigation.

**1894–1901**: Reads optional `"Irrigation_Method="`. If not found, sets `IrriMethod=0` and `MissFlag=1`.

**1903**: Writes `irrin, IrriMode, IrriIndex, IrriMethod` as header.

**1905**: Loop `kx5 = 1..irrin`.

**1907–1912**: Reads event ID with `MissFlag` handling.

**1915–1916**: Reads `Irr_Month/Day=`.

**1919–1921**: Reads amount and a method integer `jd`. If `jd` is not 0, 1, or 2, uses 0. Otherwise uses `jd` as `IrriMethod`.

**1923–1924**: Writes month/day and amount/method.

**1926**: Closes irrigation file.

---

#### Grazing (1928–2001)

**1930**: Builds path `CropGraz_<ix1>_<j>.txt`.

**1932–1938**: Opens file. On failure: `AfxMessageBox` + exit (Windows-only).

**1940–1941**: Declares `grazn, GrazMonth1/2, GrazDay1/2` and float arrays for animal types per hectare and grazing hours.

**1943–1951**: Reads `grazn` (number of grazing periods) with `MissFlag` handling.

**1955**: Writes `grazn` to file.

**1959**: Loop `kx6 = 1..grazn`.

**1965–1966**: Reads `Start_Month/Day=`.

**1969–1970**: Reads `End_Month/Day=`.

**1973–1990**: Reads animal head counts. Checks for `"Dairy_Head_per_ha="` label. If not found (old format): reads Beef, Horse, Sheep; sets Pig and Dairy to 0. If found (new format): reads Dairy, Beef, Pig, Sheep, Horse in that order.

**1992–1993**: Reads `Graz_Hours_Per_day=`.

**1995–1998**: Writes start date, end date, hours, then five animal head counts (Dairy, Beef, Pig, Sheep, Horse).

**2001**: Closes grazing file.

---

#### Grass cutting (2003–2054)

**2005**: Builds path `GrassCut_<ix1>_<j>.txt`.

**2006–2007**: Opens file. Uses `note(0, DB)` on failure (fatal error, not `AfxMessageBox` — more portable).

**2009–2010**: Declares `cutn, CutMonth, CutDay, CutPart` and `CutAmount`.

**2012**: Reads `cutn` (number of cuts per year).

**2017**: Writes `cutn` to file.

**2019**: Loop `kx7 = 1..cutn`.

**2021–2026**: Reads cut event ID with `MissFlag` handling.

**2030–2031**: Reads `Cut_Month/Day=`.

**2034–2035**: Reads `Cut_amount=` float.

**2038–2047**: Reads optional `"Cut_part="` integer. If not found, sets `CutPart=0` and `MissFlag=1`.

**2050–2051**: Writes month/day and amount/part to file.

**2054**: Closes grass cut file.

**2055–2056**: End of the year-within-cycle loop (`j`) and the rotation loop (`ix1`).

---

### Farm mode check and goto (2058–2067)

**2058**: Reads next token after all rotations.

**2059**: If token is `"----------------------------------------"` (a long dashed line), sets `FarmMode=1`.

**2060–2064**: If token is `"Crop_model_approach"`, sets `FarmMode=0` and `MissFlag=1` (the token was the next field's label, not a separator).

**2066–2067**: If `FarmMode==1`, jumps back to `NewFarmCrop:` label (line 1166) to read another farm's crop data. This is the `goto` used for multi-farm input files.

---

### Crop model approach (2069–2082)

**2069–2074**: Reads `jjd` — the crop model approach integer — with `MissFlag` handling.

**2077–2082**: If label is not `"Crop_model_approach"`, sets `jjd=0` as default instead of erroring out. The `Toendd` call is commented out.

---

### SCS runoff parameters (2086–2100)

**2086**: If `SCSuse==1` (SCS curve number method was selected in soil section):

**2088–2090**: Reads six SCS parameters: curve number `m_CN`, surface roughness, channel roughness, channel slope, channel length, land management factor. Read all on one line.

**2093–2098**: Writes them to `OUTPUT/Inputs/scspara` file.

---

### Unit system (2102–2114)

**2103**: Reads `US` (unit system flag) — 0 = metric, 1 = US customary.

**2104**: Closes the main input file `fp`.

**2106–2107**: If `US` is not 0 or 1, forces it to 0.

**2110–2114**: Writes `US` to `OUTPUT/Inter/UnitSystem` file.

**2116–2132**: Commented-out block that would have written a `CropModel` file. Removed.

---

### Soil library lookup (2135–2173)

**2142**: Builds path to soil library file: `<ROOTDIR>/Library/Lib_soil/soil_<texture_number>`.

**2144–2152**: If `Soil_Texture <= 0`, sets soil name to `"None"` and all texture defaults to 0.0 (no lookup).

**2155–2158**: Otherwise opens the soil library file and reads just the soil name and a note string. Then closes it immediately.

**2161–2172**: Commented-out block that would have also read `DClay, DPorosity, DSks, DFldcap, DWiltpt` from the library file. The library values are not used — the values read from the input file are used instead. The soil library is consulted only for the name.

**2176–2180**: Commented-out lines that would have overridden the input file values with library values.

---

### Writing the SITE and SPAN files (2187–2201)

**2187–2195**: Opens `OUTPUT/Inputs/SITE` and writes three lines: site name, simulation years / daily flag / latitude / 0 / 120, and land use / 0. Note the hard-coded `120` for longitude and `0` placeholders written to file.

**2197–2201**: Opens `OUTPUT/Inputs/SPAN` and writes `1`. This file records how many simulation spans there are.

---

### Climate file writing loop (2209–2320)

**2209**: Loop `ix2 = 1..years` — one pass per simulation year.

**2211–2214**: Picks which climate filename to use: if `if1File==0`, uses `ClimateFilename[ix2]` (one file per year); otherwise uses `ClimateFilename[1]` for all years.

**2222–2228**: Strips the directory path from the filename, keeping only the base name. Walks backward through the string looking for a backslash (ASCII 92). **Note**: uses `\\` (Windows path separator). On Linux, the separator is `/` (ASCII 47). This path stripping will fail on Linux if climate file paths use `/`.

**2239**: `if(UseID==10)` — drought prediction mode. `UseID` is set to 0 on line 2237 and never changed, so this branch is dead code. It will never execute.

**2279–2289**: Normal path (all cases since drought branch is dead): sets `id = ix2`, builds the `clim-<year>` pointer file under `OUTPUT/Inputs/`, writes the daily file name (`<base>-<year>.day`) and yearly file name (`<base>-<year>.yr`) into it.

**2293–2319**: Calls `NewCreateDailyClimateFile(...)` to actually convert/copy the climate data into DNDC's internal daily format. The drought branch (lines 2295–2312) is dead code.

---

### Reading first climate year to get initial soil temperature (2325–2355)

**2329–2334**: Opens `clim-1` pointer file and reads the daily filename `fn1` and yearly filename `fn2`.

**2338–2344**: Opens the daily file and reads the first record: day number, temperature (into `Soil_T`), max T, min T, rainfall, solar radiation, day length. The first day's temperature becomes the initial soil surface temperature.

**2346–2355**: Opens the yearly file and reads 13 floats (12 monthly means + 1 annual mean). The 13th float (`dt`) becomes `AnnualMeanT`.

---

### Soil layer calculation (2361–2390)

**2363–2365**: Sets water state variables `AcWater`, `SurfWater`, `SnowPack` all to 0.0.

**2367**: `Idc = 1 + (int)Soil_BypassFlow` — bypass flow class index.

**2369**: `Qstar` — a clay-based factor used in bypass flow calculation.

**2371**: `max_layers = 280 - 10 = 270` — maximum number of soil layers allowed.

**2372**: `TH = SOILDEPTH` — total simulated soil depth (a global constant, likely 1.0 m).

**2373–2375**: Calculates initial layer thickness `H`: uses the smaller of `0.01/Soil_Porosity` and `Sks`. If result is <= 0, calls `AfxMessageBox` and exits — **Windows-only.**

**2381–2382**: If `H * max_layers < TH`, increases `H` so the layers span the full depth.

**2384**: `Q = (int)(TH / H)` — number of layers.

**2386–2390**: If `Q > max_layers`, caps at 270 and recalculates `H`.

---

### Soil state variable calculation (2395–2461)

**2395**: `CEC = Soil_Clay * 46.0` — cation exchange capacity estimate from clay fraction.

**2396**: `m = Soil_Density * 1000 * H * 10000` — mass of soil per layer per hectare (kg/ha/layer).

**2397**: `V = H * 10000 * Soil_Porosity` — pore volume per layer per hectare (m³/ha/layer).

**2400–2406**: Declares per-layer state arrays (size 2, using index 1): `RCVL`, `RCL`, `RCR` (SOC pool sizes), `Urea`, `NH3`, `Tranr` (litter C), `OCini` (active humus C), `Dphum` (passive humus C), `CharC` (char carbon), `s_NO3`, `s_NH4`, `ClayNH4`, gas concentrations `s_NO`, `s_N2O`, `s_N2`, `s_NO2`, `s_IniNH4`.

**2408–2409**: Hydraulic variables: `PS` (pore space depth), `FLDCAPw` (field capacity in m water), `Wiltptw` (wilting point in m water), `TravelTime` (hours for water to drain through one layer), `ClayF1`, `ClayF2`.

**2412–2418**: Calculates those values from the soil parameters.

**2420**: `CN_ratios(...)` — calls a function to compute C:N ratios for all SOC pools from the passive C:N parameter.

**2423–2437**: Checks for an optional `SOCpartition.txt` file. If it exists and starts with `1`, reads custom C:N ratios for all five pools, overriding the calculated ones.

**2442–2453**: Calculates initial carbon mass in each SOC pool for layer 1: litter (`Tranr[1]`), active humus (`OCini[1]`), passive humus (`Dphum[1]`), char (`CharC[1]`), and initial NO3/NH4 in kg/ha. Also sets `RCVL`, `RCL`, `RCR` (very labile/labile/resistant litter fractions).

**2455–2456**: Splits initial NH4 into dissolved (70%) and clay-adsorbed (30%).

**2464–2474**: Sets `Urea`, `NH3`, stub values, gas pools all to 0.0.

**2476**: `id = 1, SoilRetardation = 0`.

**2478–2480**: Opens `OUTPUT/Inputs/soil-1` for writing.

**2482–2484**: `IniFrostN2O`, `IniFrostN2`, `ice` all set to 0. `Retard = 1`. If `SoilRetardation==1`, `Retard = 3` — but `SoilRetardation` is always 0 here, so always 1.

**2516–2520**: Calculates per-layer temperature profile `tt[ix3]`: uses an inverse square root decay from surface temperature toward annual mean with depth.

---

### Writing soil-1 output file (2522–2564)

**2522–2563**: Writes all calculated soil data to `OUTPUT/Inputs/soil-1` in a fixed multi-line format. Fields written include: texture code, soil name, clay/porosity/Sks/field capacity/wilting point/lateral influx, bulk density/pH/bypass class/SOC, NO3/NH4/litter/humads/humus, layer thickness/total depth/mass/volume/layer count, clay factors, bypass and retardation parameters, SOC pool sizes, water state, passive C:N/frost gas initial values, microbial index, impeding layer, slope/quality, field capacity/wilting point, SOC decay factors, initial SOC and nutrient pools, surface temperature, highest water table, SCS flag, char carbon, decomposition coefficients, and C:N ratios.

**2564**: Closes soil output file.

---

### Final file and function calls (2568–2601)

**2568–2574**: Writes `OUTPUT/Inter/SoilNewIni` with value `1` — signals that the soil initial conditions file is freshly generated.

**2577**: Calls `NewSaveInterFarmParas()` — saves farm-level intermediate parameters to files.

**2580**: Calls `NewSaveCropParas()` — saves crop parameters to files.

**2583–2585**: `#ifdef MANURE` — a commented-out call to `NewRecordManureFiles(years)`.

**2587–2600**: `#ifdef BASE` — copies all the parsed input values (site name, latitude, years, climate parameters) into the global `IData` struct. This is used when the BASE flag is defined.

**2601**: Closing `}` — end of `CreateDndcInputFiles`.

---

## Summary of Key Issues for Linux Port

| Issue | Lines | Description |
|---|---|---|
| `CString` usage | 491, 494, 611 | Windows MFC type; must be replaced with `std::string` or plain `char[]` |
| Hard-coded longitude | 626 | `120.0` is China default; should be read from input or set per region |
| Redundant fname copy | 614–616 | `ffname` is identical to `fname`; one can be removed |
| Inline forward declaration | 640 | Legal but non-standard; move to file scope |
| Fragile separator skip | 647–650 | Three `fscanf` calls skip by token count, not by line; brittle |
| Split path in CONSOLE vs GUI | 554–558 | ManureLagoon path differs; use one consistent path for Linux |
