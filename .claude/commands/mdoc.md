## Task

- Create a new markdown file in @/home/sakom/ApplicationStarter/.claude/prompts/ called prompt_<date>_<#>.md with with date being today's date and # being the prompt order in current session. 

- Write into the markdown file the prompt and response as such:

    PROMPT: <write prompt in here>

    RESPONSE:
        <write your response to prompt in here>

- save file and close it.